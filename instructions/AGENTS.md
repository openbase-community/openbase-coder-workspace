- When the user asks for a report or proposal, use the `openbase-coder-reports` skill.
- Prefer a single source of truth when coding. Do not duplicate data across files, models, generated artifacts, or config unless there is a deliberate boundary such as a generated cache or build artifact with a clear regeneration path.
- When the user says "read only mode", treat that as a persistent mode for subsequent interaction: do not make any file edits or other changes until the user explicitly confirms that specific edit.
- Before running any destructive command or public publishing command, always get a verbal confirmation from the user that says "${dangerous_confirmation_phrase}".
- "Destructive commands" include: deleting files, overwriting work, resetting or reverting changes, killing unrelated processes, changing persistent configuration, uninstalling software, modifying production data, or any operation that cannot be easily undone.
- "Public publishing commands" include: pushing commits or tags to public repos, publishing public packages, deploying public-facing services, sending emails or messages to other people, creating public links, uploading files to public destinations, or making any other kind of publicly visible change.
- If no agent thread/session ID is present in your session context for the `Agent-Thread-Id` commit trailer (the session-ID hook injects it along with its usage instructions), do not guess one, reuse an ID from elsewhere, or query Super Agents to discover your own ID; tell the user the session ID was unavailable before committing without the trailer.
- In general, when creating or updating INTERNAL team resources like notion tickets, CRM comments, and you can add your Super Agents thread ID/UUID in a way that is not too intrusive, you should do so to preserve a record that you were the one that performed the action.
- If the user asks you to restart yourself or restart the voice Codex stack, first explain that it will briefly interrupt the voice session and get a "${dangerous_confirmation_phrase}" confirmation. Then run `openbase-coder self-restart`, give one short final response, and do not use raw `launchctl` restart commands for this flow.
- Consult and maintain the workspace-level `GLOSSARY.md`. When adding docs, instructions, reports, or features that introduce or rely on recurring Openbase-specific terms, add or update concise glossary entries there rather than redefining terms differently across repos.
- The definitive source for Openbase-related docs is the Openbase Coder docs tree (`openbase-coder-workspace/cli/docs`; from `openbase-cloud-workspace`, `../openbase-coder-workspace/cli/docs`). Check and update that docs tree before adding or duplicating Openbase docs elsewhere.
